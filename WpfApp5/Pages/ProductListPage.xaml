<Page x:Class="WpfApp5.Pages.ProductListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp5.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductListPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" x:Name="FilterSp">
            <TextBlock>Поиск</TextBlock>
            <TextBox x:Name="SearchTb" TextChanged="SearchTb_TextChanged"></TextBox>
            <ComboBox x:Name="FilterCb" DataContextChanged="FilterCb_DataContextChanged">
                <ComboBoxItem>По умолчанию</ComboBoxItem>
                <ComboBoxItem>По возрастанию</ComboBoxItem>
                <ComboBoxItem>По убыванию</ComboBoxItem>
            </ComboBox>
        </StackPanel>
        <ListView Grid.Row="1" x:Name="ProductList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="2">
                        <Grid  Background="{Binding ColorDiscountAndCount}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition></ColumnDefinition>
                                <ColumnDefinition></ColumnDefinition>
                                <ColumnDefinition></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" BorderBrush="Black" BorderThickness="2" Margin="5">
                                <Image Width="200"  Source="{Binding FullPhotoPath}" ></Image>
                            </Border>

                            <Border BorderBrush="Black" BorderThickness="2" Grid.Column="1" Margin="5">
                                <StackPanel Width="900">

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock FontWeight="Bold" Text="{Binding Category.Name}"></TextBlock>
                                        <TextBlock Text="{Binding Name}"></TextBlock>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock TextWrapping="Wrap" >Описание товара:</TextBlock>
                                        <TextBlock Text="{Binding Description}"></TextBlock>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock>Производитель:</TextBlock>
                                        <TextBlock Text="{Binding Supplier.Name}"></TextBlock>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock >Поставщик:</TextBlock>
                                        <TextBlock Text="{Binding Manufacturer.Name}"></TextBlock>
                                    </StackPanel>

                                    <StackPanel  Orientation="Horizontal">
                                        <TextBlock >Ценa:</TextBlock>
                                        <TextBlock Foreground="Red" TextDecorations="Strikethrough" Text="{Binding Price}" Visibility="{Binding PriceVisibility}"></TextBlock>
                                        <TextBlock  Text="{Binding PriceOfDiscount}"></TextBlock>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock >Единица измерения:</TextBlock>
                                        <TextBlock Text="{Binding Units.Name}"></TextBlock>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock >Количество на складе:</TextBlock>
                                        <TextBlock Text="{Binding Coast}"></TextBlock>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border BorderBrush="Black" BorderThickness="2" Grid.Column="2" Width="200" Margin="5">
                                <TextBlock Text="{Binding Current_Discount}"  HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
                            </Border>
                           
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Row="0" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button x:Name="AddBtn" Click="AddBtn_Click">Добавить</Button>
            <Button x:Name="EditBtn" Click="EditBtn_Click">Редактировать</Button>
        </StackPanel>
    </Grid>
</Page>
